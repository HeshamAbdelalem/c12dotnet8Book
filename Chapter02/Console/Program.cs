
namespace Console
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Press a key console app
            //WriteLine("Please Press any key on your keyboard:");
            //ConsoleKeyInfo key = ReadKey();
            //WriteLine();
            //WriteLine($"You Pressed: {key.Key}\t  Char: {key.KeyChar}\t  Modifiers: {key.Modifiers}");
            //ReadLine();
            #endregion

            #region Arguments Check

            //WriteLine($"There are {args.Length} args in your function");


            //if (args.Length < 3)
            //{
            //    WriteLine("You Must specify two colors and cursor siz, e.g.");
            //    WriteLine("dotnet run red yellow 50");
            //    return;
            //}

            //ForegroundColor = (ConsoleColor)Enum.Parse(
            //    enumType: typeof(ConsoleColor), value: args[0],
            //    ignoreCase: true);

            //BackgroundColor = (ConsoleColor)Enum.Parse(
            //    enumType: typeof(ConsoleColor), value: args[1],
            //    ignoreCase: true);

            //CursorSize = int.Parse(args[2]);

            //foreach (string arg in args)
            //{
            //    WriteLine(arg);
            //}
            #endregion


            
        }
    }
}
